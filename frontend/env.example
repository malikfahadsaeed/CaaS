# Frontend configuration (copy to .env.local for local dev)
# Base URL of the backend chat API.
# - Docker stack / production (Caddy proxies same-origin): leave as /api/v1
# - `next dev` against a local backend on :8090: http://localhost:8090/api/v1
NEXT_PUBLIC_API_BASE_URL=/api/v1
